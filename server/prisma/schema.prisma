datasource db {
  provider = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model Event {
  id        String  @id @default(uuid())
  title     String
  allDay    Boolean @default(false)
  start     String
  end       String
  createdAt String
  updatedAt String
}

